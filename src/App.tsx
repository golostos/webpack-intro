import React from 'react'
import Tasks from './components/Tasks'

type Props = {}


function App({ }: Props) {
    // const { error, message } = useMessage()
    return <>
        <Tasks />
    </>
}

export default App